<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Fullscreen black so the WebGL orb sits on the same void as desktop.
         windowBackground stays pure black to avoid a white flash on cold start. -->
    <style name="Theme.JarvisCompanion" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
