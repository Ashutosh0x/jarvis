<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened Runtime entitlements.

  Each one is here because a specific feature stops working without it, not as
  a blanket grant:

    allow-jit / unsigned-executable-memory  V8 cannot run without them.
    allow-dyld-environment-variables        Electron sets DYLD_* when launching.
    audio-input                             Wake word and dictation.
    camera                                  Only on an explicit request.
    automation.apple-events                 "open chrome", "focus notepad".
    network.client / network.server         Search providers, and the LAN
                                            companion bridge, which listens.
    files.user-selected.read-write          Document parsing and downloads.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.automation.apple-events</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
</dict>
</plist>
